<!doctype html>
<html lang="en">
<head>
    <%- include('../partials/dashboard-head', {pagename: 'Partners'}) %>
</head>

<body>
    <%- include('../partials/dashboard-nav') %>

    <div class="container-fluid">
        <div class="row">
            <!-- sidebar -->
            <%- include('../partials/sidebar.ejs', {page: 'partners', activeParent: 'partners'}) %>

            <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-5 pb-2 mb-3">
                    <h1 class="h2">Partners CMS</h1>
                </div>

                <div class="container">
                    <div class="row">
                        <div class="col-lg-12 col-nd-12 col-sm-12 col-xs-12 px-0">
                            <a href="/dashboard/partner/add" class="btn btn-primary">Add New Partner <span class="fa fa-plus-circle"></span></a>
                        </div>

                        <div class="col-lg-12 col-nd-12 col-sm-12 col-xs-12 px-0">
                            <hr class="" />
                        </div>

                        <h3 class="col-lg-12 col-nd-12 col-sm-12 col-xs-12">Current Partners</h3>

                        <div class="col card-deck mt-3 pl-2">
                        <% for(let i=0; i < result.length; i++) { %>
                            <div class="col-sm-4 col-xs-12 px-2"><div class="border mb-4 mx-1 px-0">
                                <div class="card-body text-center">
                                    <img src="<%= result[i].postImage %>" alt="<%= result[i].name %>" class="img-fluid p-2">
                                </div>
                                <div class="card-footer justify-content-center d-flex">
                                    <a class="btn btn-primary float-left mr-3" href="/dashboard/partner/edit/<%= result[i]._id %>">Edit Partner</a>
                                    <form action="/dashboard/partner/delete/<%= result[i]._id %>?_method=DELETE" method="POST" class="form-inline float-left">
                                        <input type="hidden" name="id" value="<%=result[i]._id%>">
                                        <input type="submit" class="btn btn-danger delete-item" value="Delete">
                                    </form>
                                </div>
                            </div></div>
                        <% }%>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <%- include('../partials/dashboard-scripts') %>
</body>
</html>





